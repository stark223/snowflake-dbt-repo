
sources:
  - name: "sahil's sources"
    database: DB_ECOMM
    schema: SC_ECOMM
    tables:
      - name: USERS
      - name: SCD_EG
      - name: person_sales
      - name: PAYMENT_MODES
      - name: DIM_SALES


models:
  - name: "sahil2"
    config:
      materialized: "table"
  - name: "users_v2"
    config:
      materialized: 'table'
  - name: "users_v3"
    config:
      materialized: 'view'
  - name: "test_jinja.sql"
    config:
      materialized: "table"
      
